[tool.pytest.ini_options]
pythonpath = [
  "apps/imbs_core"
]
testpaths = [
  "apps/imbs_core/tests"
]

[tool.ruff]
line-length = 100
target-version = "py310"
extend-exclude = [".venv", "node_modules"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true
exclude = [".venv"]
