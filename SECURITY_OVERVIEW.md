# Security Overview

- JWT issuing/validation helper for service-to-service auth.
- OAuth2 provider config for external integrations.
- Zero-trust service identity checks.
- Audit logging with encoded payload snapshots.
- RBAC and permission query conditions in Frappe hooks.
