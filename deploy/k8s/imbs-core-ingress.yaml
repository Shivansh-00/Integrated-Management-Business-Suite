apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: imbs-core
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/proxy-body-size: "20m"
spec:
  rules:
  - host: imbs.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: imbs-core
            port:
              number: 80
  tls:
  - hosts:
    - imbs.example.com
    secretName: imbs-core-tls
